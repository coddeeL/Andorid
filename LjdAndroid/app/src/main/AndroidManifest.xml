<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ljdandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClockActivity"></activity>
        <activity android:name=".fileChoose" />
        <activity android:name=".edit_FileText" />
        <activity android:name=".add_FileText" />
        <activity android:name=".EmpytDefault" />
        <activity android:name=".fileActivity" />
        <activity android:name=".edit_text" />
        <activity android:name=".add_text" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>